# ChefSupport API (Spring Boot)

Java 21 + Spring Boot 3.3 service providing REST and WebSocket endpoints.

- Persistence: PostgreSQL (Flyway migrations in /db/migrations)
- Schedules: expiry checks, notifications
